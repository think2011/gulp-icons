{
  "name": "gulp-icons",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build:px": "template=px gulp && rm -rf ./dist/*.scss",
    "build:rem": "template=rem gulp && rm -rf ./dist/*.scss"
  },
  "engine": "node >= 7.6.0",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-load-plugins": "^1.5.0",
    "gulp-sass": "^3.1.0",
    "gulp.spritesmith": "^6.4.0",
    "node-sass": "^4.5.2",
    "run-sequence": "^1.2.2"
  },
  "dependencies": {
    "export-dir": "^0.1.2",
    "fs-promise": "^2.0.2"
  }
}
